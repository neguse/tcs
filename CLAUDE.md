# TinyC# (tcs) — C#サブセット → Lua 5.5 トランスパイラ

## プロジェクト概要

C#の小さなサブセットをフロントエンド言語とし、Lua 5.5ソースコードをバックエンドとするトランスパイラ。
フル C# / フル BCL ではなく、モダンな .NET / C# 開発体験を満たす最小ラインを選び、Go のようなコンパクトさを保つ。
Rider など標準的な C# ツールキット上で tcs 準拠コードかをリアルタイム確認できる Roslyn Analyzer も開発体験の中核に置く。
詳細は `objective.md` を参照。

## ディレクトリ構成

```
tcs/
├── CLAUDE.md              # このファイル
├── objective.md           # プロジェクト目標・設計文書
├── q.md                   # 未解決の質問
├── doc/
│   ├── current.md         # 現在の進捗状況
│   ├── tasks.md           # タスクリスト（フェーズ別）
│   └── done.md            # 完了ログ
├── Transpiler/            # トランスパイラ本体 (.NET)
│   ├── Transpiler.csproj
│   ├── Program.cs         # CLI エントリポイント
│   └── ...
├── Transpiler.Tests/      # xUnit テスト
│   └── ...
├── TinySystem/            # 標準ライブラリ型定義 (.NET)
│   ├── TinySystem.csproj
│   └── ...
├── TinyCs.Analyzers/      # Roslyn Analyzer PoC
├── TinyCs.Analyzers.Tests/
├── Shared/                # analyzer / transpiler / check 共有診断ルール
├── samples/               # サンプル TinyC# スクリプト
├── runtime/               # Lua側ランタイムライブラリ
├── deps/                  # 外部依存 (git submodule)
│   └── lua/               # Lua 5.5 ソース
├── tcs.slnx               # .NET ソリューション
├── run-tests.sh           # テスト実行スクリプト
└── .githooks/
    └── pre-commit         # テスト実行ゲート
```

## ビルド・テスト

### 前提

- .NET 10 SDK / C# 14
- CMake 3.12+ / C compiler (Lua 5.5 build)
- Lua 5.5 (`deps/lua/lua` or `deps/lua/lua.exe`)

### コマンド

```bash
# テスト実行（基本操作）
dotnet test

# 準拠チェック（Lua 出力なし、警告/エラーで exit 1）
dotnet run --project Transpiler -- check input.cs --ref engine-stub.cs

# トランスパイラ実行
dotnet run --project Transpiler -- input.cs -o output.lua

# Lua 5.5 ビルド
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release

# Lua でトランスパイル結果を実行
deps/lua/lua output.lua

# 全テスト（シェルスクリプト）
bash run-tests.sh
```

`run-tests.sh` / `run-tests.ps1` は Lua binary が未生成・stale・Lua 5.5 でない場合に CMake で再ビルドし、`dotnet test`、代表 sample の `tcs check`、analyzer-demo build の expected diagnostics 検証を実行する。

## 開発ワークフロー

### 作業手順

1. `doc/current.md` で現在の状態を確認
2. `doc/tasks.md` から次のタスクを選択
3. テストを先に書く（TDD）
4. 実装する
5. `dotnet test` で全テスト通過を確認
6. `doc/done.md` に完了ログを記録
7. `doc/tasks.md` から完了したタスクを削除（done.md に移動済みなので tasks.md には残さない）
8. `doc/current.md` を更新
9. コミット（1タスク = 1コミット）

### 完了ログ形式（done.md）

```markdown
### タスク名 ✓ (YYYY-MM-DD)
- [実施内容]
- [変更ファイル]
- よかったこと: [成功したアプローチ、再利用可能なパターン]
- 判断: [トレードオフ、選択理由、却下した代替案]
- 残課題: [残作業]
```

### コミットルール

- 1タスク = 1コミット（`git bisect` / `git revert` 可能に）
- テスト + 実装 + ドキュメント更新をセットで
- コミットメッセージ: `feat: T{番号} {概要}`（例: `feat: T3 if/else文のトランスパイル`）

## コード規約

### C# (.NET 10 / C# 14)

- `<TargetFramework>net10.0</TargetFramework>` / `<LangVersion>14</LangVersion>`
- nullable enable（全プロジェクト）
- ファイルサイズ上限: 600行（警告）、800行（禁止）
- テストは xUnit + FluentAssertions スタイル
- Roslyn の `Microsoft.CodeAnalysis.CSharp` パッケージを使用

### Lua 出力

- インデント: スペース2つ
- Lua 5.5 互換のみ（5.4以前の機能に依存しない）
- 生成コードにはヘッダコメントを付与: `-- Generated by TinyC# transpiler`

### テスト方針

- **セマンティックテスト**: C#入力 → トランスパイル → Lua VM実行 → 結果検証
- 出力文字列の完全一致テストは使わない（Lua出力形式の変更に弱い）
- `TranspileAndRun(csharpSource, luaExpr)` ヘルパーで統一
- Lua 5.5 バイナリ (`deps/lua/lua`) をプロセス起動して実行

## トランスパイル方針

### 基本マッピング

| C#                  | Lua 5.5                            |
|---------------------|------------------------------------|
| class               | table + metatable                  |
| struct / record struct | TCS1001 未対応診断（class / record class で代替） |
| enum                | integer 定数テーブル                |
| method              | function(self, ...)                |
| static method       | function(...)                      |
| property            | getter/setter メソッド              |
| field               | table フィールド                    |
| List\<T\>           | sequence table `{v1, v2, ...}`     |
| Dictionary\<K,V\>   | hash table `{[k]=v, ...}`         |
| lambda              | function(...) ... end              |
| interface           | 型チェックのみ（Lua出力なし）       |
| namespace           | ネストされた table モジュール        |
| string interpolation| string.format                      |

### Roslyn活用

- `CSharpCompilation` でセマンティック解析
- `IOperation` (Bound Tree) を走査してLuaコード生成
- 型情報は Roslyn から取得、自前の型システムは持たない
- Roslyn Analyzer / transpiler warning / `tcs check` は `Shared/TinyCsComplianceFacts.cs` の同じ準拠ルールを共有する
- `TCS1001`: 未対応構文 (`struct`, `record struct`, `try`, `throw`, local function, list pattern など)
- `TCS1002`: 未対応 BCL API / core library allowlist 外 member (`Math.Log`, `List.Reverse`, `Enumerable.Single` など)
- `TCS1003`: Lua table で表現できない collection null 保存

## 外部エンジン連携メモ

tcs のコア目的は engine agnostic な C# サブセット → Lua 5.5 トランスパイラであり、lub3d への直接対応は現時点の前提ではない。
lub3d 側のプロジェクト方針が変わっているため、以下の TinyCSharpGen 案は過去の検討メモとして扱う。実装タスクに戻す場合は、その時点の lub3d 側設計を読み直してから判断する。

### 旧案: lub3d API interface 生成（TinyCSharpGen）

lub3d の Generator パイプラインに乗せて C# interface を自動生成する案。

### アーキテクチャ

```
Clang AST → TypeRegistry → ModuleSpec（既存IR）
                                ├→ CBindingGen.Generate()   → .c  (既存)
                                ├→ LuaCatsGen.Generate()    → .lua (既存)
                                └→ TinyCSharpGen.Generate() → .cs  (新規)
```

### BindingType → C# 型マッピング

| BindingType       | C# 型           |
|-------------------|-----------------|
| Int               | int             |
| Int64             | long            |
| UInt32            | uint            |
| Float             | float           |
| Double            | double          |
| Bool              | bool            |
| Str               | string          |
| Void              | void            |
| Struct(_, _, lua) | class名         |
| Enum(_, lua)      | enum名          |
| Callback(p, r)    | Action/Func     |
| Vec2              | Vector2         |
| Vec4              | Vector4         |
| Ptr(inner)        | inner（透過）    |
| VoidPtr           | nint            |

### 生成物の配置

- lub3d の `Generator/TinyCSharp/TinyCSharpGen.cs` に実装
- 出力先: tcs の `TinySystem/` または `gen/` ディレクトリ
- lub3d の `Program.cs` に `TinyCSharpGen.Generate(spec)` 呼び出しを追加

### 段階的展開

1. App + Gfx + Glm で検証
2. 全モジュール展開（Audio, Imgui, Shape, Time, DebugText, ...）

## 参考リポジトリ（読み取り専用）

- `../lub3d` — 参考実装。直接対応先とは限らない
- `../lubs` — Lua linter（開発ワークフローの参考）

### lub3d から学ぶこと

- Generator/ の .NET プロジェクト構成・IModule/ModuleSpec/BindingType
- LuaCatsGen の実装パターン（TinyCSharpGen のテンプレート）
- xUnit テストの書き方
- deps/ の submodule 管理
- Lua 5.5 の組み込みパターン
- ホットリロード（`lib/hotreload.lua` + `lume.hotswap`）

### lubs から学ぶこと

- doc/ ディレクトリによるタスク管理
- done.md の完了ログ形式
- run-tests.sh のテスト実行パターン
- .githooks/pre-commit の品質ゲート
- コードメトリクス管理（ファイルサイズ、複雑度）

## 既知の制約・注意事項

- dotnet と Lua の数値精度差異（double vs Lua number）
- 文字列: C# は UTF-16、Lua は バイト列（UTF-8前提）
- struct / record struct: 現時点は TCS1001 未対応診断。値セマンティクス需要が出るまでは class / record class で代替
- null vs nil: C# の null は Lua の nil にマップ
- 型消去: ジェネリクスはコンパイル時のみ、Lua出力には型情報なし
